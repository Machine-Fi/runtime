# Public Claims

Supported claims: SDK/CLI interfaces, Robinhood and Solana receipt verification, wallet-linked settlement intents, fixture CI mode, caller-supplied live-read provider configuration, and public npm availability as `@machinefi/runtime`.

Launch copy should state what developers can install, run, and verify with this package: typed sessions, adapter-backed receipt checks, settlement intent builders, and fixture-mode CLI smoke commands.
