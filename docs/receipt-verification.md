
Receipt checks separate native chain fields from fixture and envelope metadata.

Receipt checks separate native chain fields from fixture and envelope metadata.

Receipt checks separate native chain fields from fixture and envelope metadata.
